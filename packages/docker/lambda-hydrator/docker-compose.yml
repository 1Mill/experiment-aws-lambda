version: '3.8'

services:
  app:
    image: docker.pkg.github.com/experiment-aws-lambda:${VERSION-development}
    build: .
    environment:
      NODE_ENV: develompent
    volumes:
      - ./:/app

  test:
    image: docker.pkg.github.com/experiment-aws-lambda:${VERSION-development}
    environment:
      NODE_ENV: production
